<p>
  <b>Name:</b>
  <%=h @category.name %>
  <br/>
  <br/>
  <b>Children:</b>
  <br/>
  <%#= show_children %>
  <% @category.children.each do |category| %>
    <br/>
    <%=h category.name %>
    <br/>
  <% end %>
  <%= "No Children" if @category.children.blank? %>
</p>


<%= link_to 'Edit', edit_admin_category_path(@category) %> |
<%= link_to 'Back', admin_categories_path %>